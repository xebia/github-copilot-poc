{
    // markdownlint-cli2 configuration
    "config": {
        "default": true,
        // Disable line length (was previously set to 120, but .markdownlint.json had it disabled)
        "MD013": false,
        // Disable inline HTML
        "MD033": false,
        // Disable first line heading requirement
        "MD041": false
    },
    "gitignore": true,
    "globs": [
        "**/*.md"
    ],
    "ignores": [
        "node_modules/**"
    ]
}